[package]
name = "orchestrator-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "orchestrator"
path = "src/main.rs"

[dependencies]
clap = { workspace = true }
orchestrator-core = { path = "../orchestrator-core" }
